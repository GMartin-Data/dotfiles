.ruff_cache/
__pycache__/
*.pyc

# Audit metadata — local working notes, not part of the repo
AUDIT_PROGRESS.md

# Handoff documents — design source notes, disposable once implemented
docs/handoff/

# claude plugin eval — per-run reports, regenerated on every campaign
claude/evals/results/
# runner side-effect: resumed-session transcripts written next to history.jsonl
claude/evals/**/*.jsonl
!claude/evals/**/history.jsonl
